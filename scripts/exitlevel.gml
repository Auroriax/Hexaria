//Sets some visual and audio related settings when leaving a room. That's all

background_alpha[1] = 0.05; 
audio_sound_gain(mus_menu, 1, 1000); audio_sound_gain(mus_zen, 1, 1000); audio_sound_gain(mus_beatingsun, 1, 1000);
audio_sound_gain(mus_credits, 1, 1000);
window_set_cursor(cr_default)
